以下将以图式揭露本发明的实施方式，为明确说明起见，许多实务上的细节将在以下叙述中一并说明。然而，应了解到，这些实务上的细节不应用以限制本发明。也就是说，在本发明的部分实施方式中，这些实务上的细节是非必要的。
本发明是一种对BBR关键参数的动态优化方法，该方法通过基于强化学习和ECN显式拥塞通知机制的响应式模型实现，该模型包括
BBR状态机模块：构建BBR协议的状态机，作为后续强化学习模型的基础调度框架。BBR状态机包括四个阶段：Startup启动阶段、Drain队列排空阶段、ProbeBW带宽探测阶段、ProbeRTT最小RTT探测阶段。Startup启动阶段指数增大发送速率以探测链路的瓶颈带宽，Drain队列排空阶段减小发送速率以排空Startup阶段形成的队列积压，ProbeBW带宽探测阶段是稳态阶段，持续探测链路的瓶颈带宽，并周期性进入ProbeRTT阶段，使用极低的发送速率排空瓶颈缓冲区的积压, 更新最小RTT。后续强化学习模型作用于BBR状态机的ProbeBW带宽探测阶段和ProbeRTT阶段

链路状态探测模块：BBR会持续探测链路状态特征，包括链路当前的交付速率和最小RTT、近期的RTT抖动和丢包率等，特别的，

强化学习策略模块

接收端CE标记处理模块

发送端ECN-Echo标记处理模块

奖励计算模块

如图1所示，本发明是一种基于机器学习的分布式架构下裸金属启动优化调度方法，该调度方法具体包括如下步骤：
